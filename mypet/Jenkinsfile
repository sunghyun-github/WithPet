pipeline {
    agent any
    environment {
        DOCKER_IMAGE = "mypet:latest"
    }
    stages {
        stage('Checkout') {
            steps {
                // GitHub에서 'develop' 브랜치 체크아웃
                git branch: 'develop', url: 'https://github.com/sunghyun-github/WithPet.git'
            }
        }
        stage('Build') {
            steps {
                script {
                    // Gradle 빌드 명령어
                    bat 'gradlew.bat build'
                }
            }
        }

        stage('Docker Build') {
            steps {
                script {
                    // Docker 이미지 빌드
                    bat 'docker build -t %DOCKER_IMAGE% .'
                }
            }
        }

        stage('Docker Push') {
            steps {
                script {
                    // Docker 이미지 푸시 (옵션)
                    bat 'docker push %DOCKER_IMAGE%'
                }
            }
        }

        stage('Deploy') {
            steps {
                script {
                    // Docker Compose로 컨테이너 실행
                    bat 'docker-compose up -d'
                }
            }
        }
    }
}
