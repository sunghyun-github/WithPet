pipeline {
    agent any
    environment {
        DOCKER_IMAGE = "mypet:latest"
    }
    stages {
        stage('Build') {
            steps {
                script {
                    // Gradle 빌드
                    bat './gradlew build'
                }
            }
        }
        stage('Docker Build') {
            steps {
                script {
                    // Docker 이미지 빌드
                    bat 'docker build -t %DOCKER_IMAGE% .'
                }
            }
        }
        stage('Docker Push') {
            steps {
                script {
                    bat 'docker push %DOCKER_IMAGE%'
                }
            }
        }
        stage('Deploy') {
            steps {
                script {
                    // docker-compose 명령을 실행하여 .env 파일을 자동으로 로드하고 배포
                    bat 'docker-compose up -d'
                }
            }
        }
    }
}
